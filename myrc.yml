apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx-RC
spec:
  replicas: 2
  selector:
    app: myapp
    version: 2.3.2
  template:
    metadata:
      labels:
        app: myapp
        version: 2.3.2
    spec:
      containers:
        - name: nginx-container
          image: nginx
          ports:
            - containerPort: 80